name: Kur Guncelle Cron

on:
  schedule:
    - cron: "15 13 * * *" # 16:15 TR
  workflow_dispatch:

jobs:
  update-kur:
    runs-on: ubuntu-latest

    steps:
      - name: Render uyanıyor
        run: |
          echo "☀️ Render uyandırılıyor..."
          curl -s https://kur-guncelle-api.onrender.com || true
          sleep 25

      - name: Kur guncelle
        run: |
          echo "💸 KUR GÜNCELLEME BAŞLADI: $(date)"
          curl -v -X POST https://kur-guncelle-api.onrender.com/kur-guncelle \
          -H "x-api-key: ${{ secrets.API_KEY }}"
